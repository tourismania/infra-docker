# Issue #22 — Watchdog не детектирует зависший getUpdates: getMe проходит, бот молчит (follow-up #16)

GitHub Issue: https://github.com/tourismania/infra-docker/issues/22

## Проблема

Watchdog, добавленный в #16, проверяет живость через `bot.get_me()`. 2026-07-17 бот снова завис: после единичного `ConnectTimeout` через xray (07:28 UTC) long-polling `getUpdates` полностью остановился, но `getMe` продолжал успешно проходить — watchdog каждые 3 минуты рапортовал «связь с Telegram API в порядке», рестарта не произошло, бот не отвечал пользователям.

Из логов: после 07:28 в httpx-логах есть только `POST .../getMe 200 OK` и ни одной строки `getUpdates` (при живом поллинге они появляются каждые ~10 сек). Контейнер `Up`, процесс жив.

## Причина

`getMe` и `getUpdates` используют разные HTTP-клиенты (у `get_updates` отдельный пул с `connection_pool_size=1`). Зависает именно соединение поллинга — watchdog проверяет не тот канал. Проверка `getMe` не покрывает сценарий «туннель для getUpdates мёртв, новое соединение для getMe устанавливается успешно».

## Acceptance Criteria

- [x] Watchdog проверяет живость самого поллинга: фиксируется timestamp последнего успешного `getUpdates` (обёртка над `HTTPXRequest` или аналог); если он старше N × интервал поллинга (например, 90–120 сек), проверка считается проваленной.
- [x] Существующий порог `WATCHDOG_MAX_FAILURES` подряд сохраняется — после его достижения процесс завершается через `os._exit(1)`, контейнер перезапускается политикой `restart: unless-stopped`.
- [x] Порог «свежести» getUpdates конфигурируется через переменную окружения с разумным дефолтом.
- [x] Логи явно показывают возраст последнего успешного `getUpdates` при каждой проверке watchdog.
- [x] Обновлена документация (`CLAUDE.md`), создан `docs/issues/22.md`.

## Negative Constraints

- Не трогать бизнес-логику опросника (`ConversationHandler`, тексты, порядок вопросов) — только сетевой/инфраструктурный слой.
- ~~Не убирать существующую проверку `getMe` без необходимости~~ — **пересмотрено в code review** (см. ниже): проверка `getMe` удалена, т.к. она стала избыточной.
- Не добавлять новые внешние зависимости (`requirements.txt`: `python-telegram-bot[job-queue]`).
- Не рестартовать по единичному блипу — порог из нескольких проваленных проверок подряд обязателен.
- Поддержка работы без прокси (`XRAY_HTTP_PROXY=` пустой) должна сохраниться.

## Relationship

- Related to #16 (та же проблема; watchdog из #16 закрыл только часть сценариев).

## Пересмотр решения (code review PR #23)

Первая версия PR добавляла проверку свежести `getUpdates` **вдобавок** к существующей `get_me()` — оба сигнала считались в общий счётчик неудач. В ревью (kirin-bear) справедливо указано: раз мы уже проверяем именно тот канал, который нужен (`getUpdates`), отдельная проверка `get_me()` ничего не добавляет — она была лишь суррогатом для "API вообще доступен", введённым в #16 до того, как появился прямой способ проверить сам поллинг. Если `getUpdates` регулярно завершается (даже без новых апдейтов, long-poll всё равно отвечает), это уже доказывает доступность API; если завис — не важно, отвечает ли `get_me()` из отдельного соединения, бот всё равно не видит сообщений пользователей.

Решение: `get_me()` из watchdog убран, единственный сигнал — возраст последнего успешного `getUpdates`. Это одновременно проще (один сигнал, меньше кода, не нужен `asyncio.wait_for`/`WATCHDOG_TIMEOUT_SECONDS`) и точнее (нет случая, когда `get_me()` маскирует зависший поллинг, как в исходном инциденте).

## Реализация

- `services/telegram-bot/telegram_bot.py`:
  - `_GetUpdatesFreshnessTracker(HTTPXRequest)` — переопределяет `do_request`, фиксируя `time.monotonic()` в модульной переменной `_last_get_updates_at` при каждом завершившемся (HTTP 200) запросе. Класс используется исключительно для `getUpdates` — в `main()` он подключается через `ApplicationBuilder.get_updates_request(...)`, который по устройству PTB и раньше был отдельным HTTP-клиентом от общего (см. `Причина`), так что это не третье соединение, а замена уже существующего дефолтного объекта на инстанс, умеющий сообщать нам о своей активности.
  - `.get_updates_request(...)` заменил прежние `.get_updates_connect_timeout()/.get_updates_read_timeout()/.get_updates_pool_timeout()` — ApplicationBuilder не даёт смешивать готовый request-инстанс с этими билдерами, поэтому таймауты (10/30/10, без изменений) переехали в конструктор трекера. Прокси по-прежнему берётся из `HTTP_PROXY`/`HTTPS_PROXY` (env), явный `proxy=` не передаётся — поведение без прокси не изменилось.
  - `watchdog_check` проверяет только возраст `_last_get_updates_at` против `WATCHDOG_GETUPDATES_MAX_AGE_SECONDS`; `get_me()`-проверка и связанный с ней `WATCHDOG_TIMEOUT_SECONDS` удалены как избыточные (см. `Пересмотр решения`). Порог `WATCHDOG_MAX_FAILURES` и защита от единичного блипа не изменились. В лог на каждой проверке пишется возраст последнего `getUpdates` в секундах.
  - Новая переменная окружения `WATCHDOG_GETUPDATES_MAX_AGE_SECONDS` (по умолчанию `120`). `WATCHDOG_TIMEOUT_SECONDS` удалена как неиспользуемая.
- `CLAUDE.md` — обновлена заметка про сервис `telegram-bot`.
- `services/telegram-bot/test_watchdog.py` — юнит-тесты на watchdog (см. `Как проверить`).

## Как проверить

### Автоматически (юнит-тесты)

Без новых зависимостей — только `python-telegram-bot`, который и так нужен боту.

```bash
cd services/telegram-bot
python3 -m unittest test_watchdog -v
```

Или внутри контейнера, если стек уже поднят:

```bash
docker compose exec telegram-bot python -m unittest test_watchdog -v
```

Покрывает: `_GetUpdatesFreshnessTracker` обновляет метку только на HTTP 200 и не трогает её на прочих кодах; `watchdog_check` — не рестартует на единичном блипе, рестартует ровно после `WATCHDOG_MAX_FAILURES` подряд неудач через `os._exit(1)`, и сбрасывает счётчик, когда `getUpdates` снова свежий.

Юнит-тесты проверяют логику watchdog в изоляции — они не гоняют реальный `getUpdates`/xray и не доказывают поведение всего стека целиком. Для этого — ручная проверка ниже.

### Вручную (воспроизведение реального сценария из issue)

Юнит-тесты не могут сами по себе зависнуть на реальном сетевом уровне — чтобы убедиться, что контейнер `telegram-bot` действительно перезапускается policy `restart: unless-stopped`, когда `getUpdates` перестаёт отвечать, а туннель "тихо" висит (а не рвётся с ошибкой):

1. Временно уменьшить пороги в `.env`, чтобы не ждать часами (дефолты — 180с/120с/3 попытки):
   ```
   WATCHDOG_INTERVAL_SECONDS=15
   WATCHDOG_GETUPDATES_MAX_AGE_SECONDS=20
   WATCHDOG_MAX_FAILURES=2
   ```
2. Поднять стек: `docker compose up -d telegram-bot xray`.
3. Убедиться, что всё штатно: `docker compose logs -f telegram-bot` каждые ~15с показывает `Watchdog: getUpdates отвечал Xс назад — в порядке`.
4. Смоделировать именно "тихий" обрыв туннеля (не разрыв соединения, а зависание без FIN/RST, как в инциденте 2026-07-17): `docker compose pause xray`. Это замораживает процесс в контейнере через cgroup freezer — TCP-сокеты остаются открытыми, но данные никуда не идут, что ближе всего к реальному сценарию, чем `docker compose stop` (который закрывает соединение явно и getUpdates упадёт с понятной ошибкой сразу, а не зависнет).
5. Смотреть `docker compose logs -f telegram-bot`: в течение ~(`WATCHDOG_GETUPDATES_MAX_AGE_SECONDS` + `WATCHDOG_INTERVAL_SECONDS` × `WATCHDOG_MAX_FAILURES`) секунд (в примере выше — до ~50с) должны появиться `ERROR: getUpdates не отвечал ...`, затем `CRITICAL: ... принудительный выход`, и `docker compose ps` покажет, что контейнер `telegram-bot` пересоздан/перезапущен (новый uptime, в логах заново `Бот запущен ✅`).
6. Убрать: `docker compose unpause xray`, вернуть пороги в `.env` к дефолтам (или убрать переопределения).

Это и есть прямое воспроизведение бага из issue: раньше на шаге 5 `get_me()` продолжал бы отвечать через новое соединение и watchdog писал бы «в порядке» бесконечно — теперь единственный сигнал это возраст `getUpdates`, и он корректно ловит зависание.
